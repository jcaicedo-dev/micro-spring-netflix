spring:
  application:
    name: micro-config-server
  cloud:
    config:
      server:
        git:
          uri: git@bitbucket.org:ingjmcaicedo/micro-config-properties.git
---

spring:
  profiles: docker
  cloud:
    config:
      server:
        git:
          uri: ${PROPERTIES_GIT_REPO}
